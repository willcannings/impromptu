component 'foo' do
	namespace 'Foo'
end

component 'foo.one' do
	requires 'foo.two', 'rfc.one'
	folder('components', 'foo_one').provides :A, :B
end

component 'foo.two' do
	namespace 'Two'
	folder('components', 'foo_two').provides :C, :D
end

component 'rfc.one' do
	folder('components', 'rfc_one').provides :E
	namespace 'RFC', :file => 'components/rfc_one/rfc'
end

component 'rfc.two' do
	folder('components', 'rfc_two').provides :F, :'RFC822::G'
end
