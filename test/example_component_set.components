component 'test' do
	namespace 'Test'
end

component 'test.one' do
	requires 'test.two', 'rfc.one'
	folder('components', 'testone').provides :A, :B
end

component 'test.two' do
	namespace 'Two'
	folder('components', 'testtwo').provides :C, :D
end

component 'rfc.one' do
	folder('components', 'rfcone').provides :E
	namespace 'RFC'
end

component 'rfc.two' do
	folder('components', 'rfctwo').provides :F, :'RFC822::G'
end
